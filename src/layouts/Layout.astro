<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <!-- Bloquear indexación -->
    <meta name="robots" content="noindex, nofollow" />

    <!-- SEO básico -->
    <title>Turin.dev | Agencia de Desarrollo Web Profesional</title>
    <meta
      name="description"
      content="En Turin.dev creamos lugares web modernos, rápidos y optimizados. Agencia de desarrollo web con soluciones en diseño, frontend, backend y SEO."
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    
  </head>
  <body class="bg-black overflow-x-hidden">
    <!-- Cursor glow effect -->
    <div id="cursor-glow" class="cursor-glow"></div>
    <slot />
    
    <!-- GSAP Scripts - Using different CDN for better compatibility -->
    <script is:inline src="https://unpkg.com/gsap@3.12.2/dist/gsap.min.js"></script>
    <script is:inline src="https://unpkg.com/gsap@3.12.2/dist/ScrollTrigger.min.js"></script>
    <script is:inline src="https://unpkg.com/gsap@3.12.2/dist/Draggable.min.js"></script>
    
    <!-- Main JavaScript -->
    <script is:inline src="/main.js"></script>
  </body>
</html>

<style>
  html {
    overflow-x: hidden;
  }
  
  body {
    font-family: Inter, Inter Prevent Layout Shift, system-ui, sans-serif;
    background-color: #020617;
    color: #fff;
    background-image: url(/images/bg.avif);
    background-position: 0 0;
    background-repeat: repeat-y;
    background-size: auto;
    transition: background-color .3s, color .3s;
    overflow-x: hidden;
    width: 100%;
    max-width: 100vw;
  }

  /* Cursor glow effect */
  .cursor-glow {
    position: fixed;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(34, 197, 94, 0.15) 0%, rgba(16, 185, 129, 0.08) 40%, rgba(34, 197, 94, 0) 70%);
    border-radius: 50%;
    pointer-events: none;
    z-index: -1;
    will-change: transform;
    transform: translate(-50%, -50%);
    left: 50%;
    top: 50%;
    transition: opacity 0.3s ease;
    opacity: 0;
  }

  @media (hover: hover) and (pointer: fine) {
    .cursor-glow {
      opacity: 1;
    }
  }

  /* Estados iniciales para GSAP */
  .fade-up, .fade-left, .fade-right, .scale-up, .stagger-item {
    opacity: 0;
  }

  /* Prevent horizontal scroll globally */
  * {
    box-sizing: border-box;
  }
  
  *:not(.tech-carousel) {
    max-width: 100%;
  }
  
  /* Ensure all sections respect viewport width */
  section, main, div {
    max-width: 100vw;
    overflow-x: hidden;
  }
  
  /* Fix for absolute positioned elements */
  .absolute {
    max-width: 100vw;
  }

</style>

